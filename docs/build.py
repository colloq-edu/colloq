#!/usr/bin/env python3
"""Generate static public guides and their search index. Python standard library only."""
from pathlib import Path
import re, html, json
out=Path(__file__).resolve().parents[1] / 'site' / 'docs'
pages=[]
def add(slug,group,title,desc,body): pages.append(dict(slug=slug,group=group,title=title,desc=desc,body=body))
def code(s): return '<pre><code>'+html.escape(s.strip())+'</code></pre>'
add('index','Начните здесь','Одна ссылка. Всё занятие.','Руководство по Colloq: от первого занятия до собственного сервера.', '''
<div class="intro-note"><span class="eyebrow">Документация</span><p>Colloq объединяет общий Python-ноутбук, лекционные слайды и отдельные ответы студентов. Выберите свой маршрут — от подготовки занятия до восстановления сервера.</p></div>
<div class="routes">
<a class="route" href="getting-started.html"><span class="route-number">01 / ПРЕПОДАВАТЕЛЮ</span><h2>Провести занятие</h2><p>Создать комнату, выбрать правила и пригласить группу.</p><span class="route-arrow" aria-hidden="true">↗</span></a>
<a class="route" href="students.html"><span class="route-number">02 / СТУДЕНТУ</span><h2>Присоединиться</h2><p>Войти по ссылке, работать в тетради и забрать материалы.</p><span class="route-arrow" aria-hidden="true">↗</span></a>
<a class="route" href="install.html"><span class="route-number">03 / АДМИНИСТРАТОРУ</span><h2>Запустить свой сервер</h2><p>Установить выпуск, настроить адрес и проверить исполнение.</p><span class="route-arrow" aria-hidden="true">↗</span></a>
</div>
<h2 id="room">Как устроена комната</h2><p>Преподаватель создаёт занятие на своём сервере Colloq. Студенты открывают его ссылку и вводят имя. На обычном семинаре у группы одна тетрадь, общие файлы и одно Python-ядро: переменная из первой ячейки доступна в следующей.</p>
<figure class="workspace"><img src="/img/workspace@1x.webp" width="1440" height="1000" alt="Комната Colloq: файлы и участники слева, общий ноутбук в центре, оракул справа" loading="lazy"><figcaption>Рабочий экран занятия. Доступные действия определяет преподаватель.</figcaption></figure>
<h2 id="choose">Подготовьте нужный формат</h2><div class="link-rows"><a href="notebook.html"><strong>Семинар</strong><span>Писать и запускать Python вместе →</span></a><a href="lectures.html"><strong>Лекция</strong><span>Показывать PDF, писать от руки, подключить планшет →</span></a><a href="council.html"><strong>Консилиум</strong><span>Получить отдельные ответы и обсудить их с группой →</span></a><a href="publishing.html"><strong>После занятия</strong><span>Опубликовать шаги решения и собрать курс →</span></a></div>
<h2 id="status">Что важно знать до старта</h2><p>Colloq разворачивается на вашем сервере. Этот сайт содержит документацию и описание продукта, а учебная комната открывается по адресу, который выдаёт преподаватель.</p><p>Для исходников и релизного пакета пока нужен доступ к закрытому репозиторию или комплект от владельца. Инструкции для установки описывают версионированный процесс с манифестом и образами; они не заменяют проверку конкретного выпуска на вашей машине. Для локального знакомства есть <a href="development.html">режим разработки</a>.</p><p>Разные комнаты получают отдельные ядра и файловые каталоги. Участники одной комнаты разделяют среду исполнения. Подробности и ограничения — в <a href="security.html">руководстве по изоляции</a>.</p>
''')
add('getting-started','Начните здесь','Первое занятие','От входа в панель до ссылки, по которой студенты откроют ваше занятие.', '''
<h2 id="access">1. Войдите как преподаватель</h2><p>Откройте личную ссылку входа, полученную от владельца сервера. Она имеет вид <code>https://ваш-сервер/admin/k/…</code> и открывает панель преподавателя. Пароль и почтовое подтверждение не нужны.</p><p>Если вы впервые настраиваете собственный сервер, откройте <code>/admin</code> и используйте setup-token. Он хранится в <code>DATA_DIR/setup-token</code>; в стандартной установке k3s это <code>/var/lib/colloq/data/setup-token</code>. Завершите первичную настройку владельца. Этот же ключ позволяет восстановить доступ позже.</p><aside class="callout"><strong>Две разные ссылки</strong><p>Ссылка <code>/admin/k/…</code> или <code>/admin/t/…</code> даёт административный доступ. Группе отправляйте только ссылку занятия <code>/s/…</code>. Не показывайте личный ключ на проекторе.</p></aside>
<p>В этой документации названия кнопок приведены для русского интерфейса. Владелец может <a href="language.html">выбрать русский или английский</a> для всего сервера.</p>
<h2 id="create">2. Создайте занятие</h2><p>В панели откройте список <strong>«Занятия»</strong> и форму нового занятия. Задайте понятное название, выберите доступное Python-окружение и формат.</p><div class="table-wrap"><table><thead><tr><th>Формат</th><th>Начальные правила</th><th>Когда выбрать</th></tr></thead><tbody><tr><td>Обычный</td><td>Группа редактирует общую тетрадь и запускает код.</td><td>Практика и совместный разбор.</td></tr><tr><td>Лекция</td><td>Редактирование, исполнение и управление общим экраном у преподавателя.</td><td>Объяснение с демонстрацией.</td></tr><tr><td>Консилиум</td><td>Правила лекции; открытие ячейки выдаёт каждому свой лист.</td><td>Задача с последующим обсуждением ответов.</td></tr></tbody></table></div><p>Это начальные наборы <a href="room-rules.html">правил</a>. Их можно менять по ходу занятия. Доступность оракула — выключен, подсказки или полный ответ — выбирается при создании; отдельного переключателя для её изменения после создания нет.</p>
<h2 id="prepare">3. Подготовьте рабочее место</h2><ol><li>Добавьте код и пояснения в тетрадь; при необходимости загрузите ноутбук и данные.</li><li>Запустите подготовительные ячейки. Убедитесь, что нужные библиотеки доступны в выбранном окружении.</li><li>Проверьте права студентов: редактирование, исполнение, файлы, оракул и общий экран.</li><li>Откройте ссылку занятия в отдельном приватном окне и войдите как студент. Проверьте именно этот вид перед началом пары.</li></ol><p>Для лекции заранее загрузите PDF и проверьте <a href="lectures.html">проектор и пульт</a>. Для Консилиума выполните общий подготовительный код до открытия задания.</p>
<h2 id="invite">4. Пригласите группу</h2><p>Скопируйте ссылку занятия из панели и отправьте её в чат группы. Студенту достаточно открыть её в браузере и ввести имя. Устанавливать Python на студенческие компьютеры не требуется: код выполняется на сервере.</p><p>Если в ссылке оказался <code>localhost</code>, сначала исправьте <a href="networking.html">внешний адрес сервера</a>. Проверьте открытие ссылки с другой сети, а не только с преподавательского ноутбука.</p>
<h2 id="finish">5. Завершите занятие</h2><p>Нажмите <strong>«Закончить занятие»</strong> в комнате или в списке занятий. Студенты смогут перечитать материалы, файлы, историю терминала и ответы оракула, но активные действия останутся преподавателю. <strong>«Продолжить занятие»</strong> вернёт исходные правила.</p><p>Для отдельной страницы чтения используйте <a href="publishing.html">публикацию</a>. Завершение занятия, публикация и удаление комнаты — разные действия.</p>
<h2 id="team">Доступ других преподавателей</h2><p>Владелец добавляет преподавателя на странице <strong>Who can teach</strong>: имя и email используются для идентификации, Colloq не отправляет письма. Передайте созданную ссылку входа лично. При утрате или раскрытии ссылки смените ключ в панели: прежняя ссылка и открытые с ней сессии перестанут работать.</p>
''')
add('students','Начните здесь','Студенту','Как войти в комнату, участвовать в занятии и вернуться к материалам.', '''
<h2 id="join">Вход по ссылке</h2><p>Откройте ссылку <code>/s/…</code>, которую дал преподаватель, и введите имя. Аккаунт, email и локальный Python не нужны. Работайте в том же браузере: часть вашей идентичности и локальные черновики связаны с ним.</p><p>Адрес <code>/admin</code> предназначен для преподавателя. Страница <code>colloq.ru/docs/</code> — справка, она не подключает к занятию. Если у вас есть только адрес сервера, попросите преподавателя выдать ссылку конкретной комнаты.</p>
<h2 id="workspace">Что на экране</h2><ul><li><strong>Тетрадь</strong> — код, текст, результаты запуска и курсоры участников.</li><li><strong>Файлы</strong> — данные и документы комнаты. Открытый у вас файл и документ на общем экране могут различаться.</li><li><strong>Оракул</strong> — вопросы и ответы ИИ, если он включён для занятия.</li><li><strong>Участники</strong> — люди, которые сейчас подключены.</li><li><strong>Терминал</strong> — общие команды и их вывод, если разрешён запуск кода.</li></ul><p>На узком экране боковые области открываются как панели. Закрывайте их кнопкой или клавишей <kbd>Esc</kbd>, чтобы вернуться к тетради.</p>
<h2 id="participate">Работа в общей тетради</h2><p>На обычном семинаре правки и вывод ячеек видны всей группе. Запуски идут через общее ядро: изменение переменной или файла влияет на следующих участников. Прежде чем перезапускать ядро или очищать вывод, согласуйте это с преподавателем.</p><p>Если действие недоступно, прочитайте пояснение рядом с ним. Преподаватель мог оставить его себе, временно закрыть ячейку или завершить занятие. Обновление страницы не меняет эти правила.</p>
<h2 id="own-answer">Отдельный ответ в Консилиуме</h2><p>Когда преподаватель открывает задание в режиме Консилиума, редактируйте свой лист и отправляйте ответ кнопкой <strong>«Сдать»</strong>. До показа преподавателем группа не видит ваш ответ. Преподаватель может ответить вам и показать выбранное решение всем.</p><p>Личный лист не означает личное Python-ядро. Код выполняется в среде всей комнаты. Не изменяйте общие данные на месте, если задача предполагает независимые результаты. Подробнее — <a href="council.html">Консилиум</a>.</p>
<h2 id="reading">После занятия</h2><p>По ссылке завершённой комнаты можно перечитать сохранённую работу. По ссылке <code>/p/…</code> открывается опубликованный материал: выбранные шаги решения, код и выводы. В публикации код можно копировать, а тетрадь — скачать в <code>.ipynb</code>; запустить её на этой странице нельзя.</p><p>Ссылка курса <code>/c/…</code> объединяет опубликованные занятия и план будущих тем. Сохраните её в закладках. Файлы данных из живой комнаты автоматически в публикацию не включаются — для повторного запуска уточните у преподавателя, где их получить.</p>
<h2 id="connection">Если связь прервалась</h2><p>Дождитесь переподключения и проверьте, появился ли ваш последний текст. Соединение, вывод и исполнение зависят от доступности сервера; закрытие вкладки не является командой остановки Python. Не отправляйте один и тот же запуск много раз. Если сервер недоступен, сообщите преподавателю адрес страницы, время и текст ошибки, без личных ссылок входа.</p>
''')
add('room-rules','Провести занятие','Правила комнаты','Управляйте тем, кто редактирует, запускает код и меняет общую работу.', '''
<h2 id="presets">Формат задаёт начальные правила</h2><p>«Обычный», «Лекция» и «Консилиум» — наборы начальных значений. Правила доступны при создании и в настройках комнаты; сервер проверяет их для каждого действия. Преподаватель может открыть отдельную ячейку группе, сохранив остальную тетрадь закрытой.</p>
<h2 id="rules">Что можно настроить</h2><div class="table-wrap"><table><thead><tr><th>Настройка</th><th>Что она означает</th></tr></thead><tbody><tr><td>Открытая ячейка</td><td>Общий редактируемый текст или личный лист каждому в Консилиуме.</td></tr><tr><td>Печатать в ячейках</td><td>Все или только преподаватель.</td></tr><tr><td>Запускать код</td><td>Все, по одной ячейке на участника одновременно или преподаватель. Распространяется также на запуск файлов и терминал.</td></tr><tr><td>Менять состав тетради</td><td>Все, только добавление или преподаватель. При «только добавлять» студент не может удалить даже свою ячейку.</td></tr><tr><td>Показывать документ комнате</td><td>Кто управляет общим экраном; личный просмотр остаётся доступен.</td></tr><tr><td>Создавать и редактировать файлы</td><td>Все или преподаватель. Скачивание не ограничено этим правилом; удаление и переименование остаются преподавателю.</td></tr><tr><td>Оракул правит файлы сам</td><td>Режим «Сделать» для всех, преподавателя или никого.</td></tr><tr><td>Вопросов оракулу в час</td><td>Предел на участника, не выше серверного. Не ограничивает преподавателя.</td></tr><tr><td>Промежуток между вопросами</td><td>Не короче серверного интервала. Не ограничивает преподавателя.</td></tr><tr><td>Смотреть ленту версий</td><td>Кто видит предыдущие версии тетради и авторов изменений.</td></tr><tr><td>Перезапускать ядро</td><td>Сброс общих переменных; текст ячеек и файлы сохраняются.</td></tr><tr><td>Очищать общие результаты</td><td>Все выводы, терминал и лента оракула. Очистка отдельной ячейки связана с правом её редактирования.</td></tr></tbody></table></div>
<h2 id="locks">Замок на ячейке</h2><p>Преподаватель выбирает одно из трёх состояний: <strong>закрыта</strong> — работает преподаватель; <strong>открыта</strong> — общий текст для группы в рамках правил; <strong>Консилиум</strong> — отдельные ответы студентов. Настройка «Открытая ячейка» определяет обычное действие кнопки замка; все три состояния доступны через меню.</p>
<h2 id="boundaries">Правила действий и исполнение Python</h2><aside class="callout"><strong>Учитывайте сочетание прав</strong><p>Если студент может редактировать код, преподаватель может случайно запустить уже изменённый текст: источник читается, когда очередь доходит до ячейки. Для строгой лекции ограничьте и редактирование, и запуск.</p></aside><p>Запущенный Python имеет доступ к файлам комнаты. Запрет загрузки или редактирования через панель не мешает разрешённому коду записать файл через <code>open(...)</code>. Правила интерфейса не создают отдельную файловую среду для каждого участника.</p>
<h2 id="moderation">Завершение и модерация</h2><p>«Закончить занятие» временно оставляет активные действия преподавателю, сохраняя настройки и материалы. «Продолжить занятие» возвращает прежние правила. Пустая комната означает отсутствие подключённых людей, а не завершённое занятие.</p><p>Блокировка участника действует 24 часа и привязана к отметке в браузере. Приватное окно может её обойти. Это инструмент против срыва занятия, а не проверка личности или контроль экзамена.</p>
''')
add('notebook','Провести занятие','Тетрадь, файлы и терминал','Общая рабочая среда для Python: что разделяется, что сохраняется и как запускать код.', '''
<h2 id="cells">Ячейки и общий документ</h2><p>Тетрадь состоит из ячеек с Python-кодом и текстовых пояснений. Правки синхронизируются между участниками. Под ячейкой появляются результаты запуска: текст, таблицы, графики или ошибка Python. Доступные действия зависят от <a href="room-rules.html">правил комнаты</a> и состояния замка.</p><div class="table-wrap"><table><thead><tr><th>Клавиши в редакторе кода</th><th>Действие</th></tr></thead><tbody><tr><td><kbd>Shift</kbd> + <kbd>Enter</kbd></td><td>Запустить и перейти дальше.</td></tr><tr><td><kbd>Ctrl</kbd> / <kbd>⌘</kbd> + <kbd>Enter</kbd></td><td>Запустить текущую ячейку.</td></tr><tr><td><kbd>Alt</kbd> + <kbd>Enter</kbd></td><td>Запустить и добавить следующую ячейку.</td></tr></tbody></table></div><p>Кнопки запуска подходят и для устройств без клавиатуры. Импортируйте <code>.ipynb</code>, когда хотите продолжить подготовленную тетрадь; экспортируйте его, чтобы сохранить переносимую копию кода и результатов.</p>
<h2 id="kernel">Одно ядро на комнату</h2><p>Все ячейки комнаты используют общую память Python. Выполните подготовку один раз, затем используйте значения в следующих ячейках:</p>'''+code('''# Подготовка преподавателя
numbers = [2, 4, 6]

# Следующая ячейка той же комнаты
sum(numbers) / len(numbers)''')+'''<p>Порядок исполнения важнее положения ячейки в тетради. Если переменная не определена, выполните ячейку, где она создаётся. Повторный запуск может повторно записать данные или изменить уже изменённый объект.</p><p>Перезапуск ядра стирает переменные для всех. Текст тетради и файлы сохраняются. После перезапуска выполняйте подготовительные ячейки заново. Перезагрузка страницы и перезапуск Python — разные операции.</p>
<h2 id="files">Работа с файлами</h2><p>В панели файлов есть дерево каталогов. Файл можно загрузить с компьютера, открыть, переместить в папку и скачать. Создание и редактирование определяются правилами; переименование и удаление доступны преподавателю. Текстовые файлы можно редактировать совместно.</p><p>Python видит рабочие файлы этой комнаты. Используйте относительные пути, чтобы тетрадь оставалась переносимой:</p>'''+code('''from pathlib import Path

Path("results").mkdir(exist_ok=True)
Path("results/answer.txt").write_text("42\\n", encoding="utf-8")''')+'''<p>Скачанный <code>.ipynb</code> не является резервной копией всех файлов сервера. Перед самостоятельной работой заберите также данные, изображения и другие зависимости кода.</p>
<h2 id="terminal">Общий терминал</h2><p>Терминал исполняет команды в среде комнаты и показывает общую историю вывода. Правило «Запускать код» распространяется и на него. Команда <code>python analysis.py</code> запускает отдельный процесс Python: переменные ноутбука туда автоматически не переходят, а файлы комнаты остаются общими.</p><p>Производственный образ имеет защищённую корневую файловую систему, а выход в сеть комнаты по умолчанию закрыт, кроме DNS. Установка пакетов из интернета во время занятия может не работать. Подготовьте библиотеки заранее через <a href="environments.html">выпуск окружения</a>.</p>
<h2 id="persistence">Что переживает остановку</h2><p>Ноутбук, файлы и сохранённые материалы хранятся на диске. Python-память и процессы зависят от жизни ядра. Обновление сервера или согласованная резервная копия останавливают ядра и теряют их память. Удаление диска сервера уничтожит и файлы, если нет внешней <a href="backup-restore.html">резервной копии</a>.</p>
''')
add('oracle','Провести занятие','Оракул','Настройте помощь ИИ, ограничения вопросов и работу с файлами.', '''
<h2 id="enable">Включение на сервере и в занятии</h2><p>Владелец задаёт провайдера, адрес API, модель и ключ на странице <strong>«Оракул»</strong>. Настройки панели могут переопределять переменные окружения сервера. Используется совместимый с OpenAI API провайдер. Для провайдеров, требующих авторизацию, нужен ключ; Ollama и vLLM могут работать без ключа с доступным серверу endpoint и корректной моделью.</p><p>При создании занятия выбирается доступность: <strong>выключен</strong>, <strong>подсказки</strong> или <strong>полный ответ</strong>. Позже переключателя этой настройки нет. Если для контрольной ИИ не нужен, создайте отдельную комнату с выключенным оракулом.</p>
<h2 id="ask">Вопросы и контекст</h2><p>Откройте панель оракула и задайте конкретный вопрос: какую ячейку разбираете, что ожидали и что получили. Код и контекст комнаты могут включаться в запрос, поэтому не помещайте в тетрадь API-ключи или данные, которые нельзя передавать выбранному провайдеру.</p><p>Ответы появляются в ленте комнаты. Проверяйте предлагаемый код и объяснения: ответ модели может быть неверным, а режим подсказок и дополнительные инструкции не гарантируют поведение модели.</p>
<h2 id="agent">Режим «Сделать»</h2><p>Если разрешено правило <strong>«Оракул правит файлы сам»</strong>, оракул может читать, создавать и изменять файлы, запускать код. Доступ может быть у всех, только у преподавателя или ни у кого. В режиме «Сделать» разрешённые изменения файлов и ячеек применяются сразу, в пределах прав автора запроса. В режиме вопроса предложенные изменения ячеек требуют принятия. Проверьте выбранный режим до отправки задания.</p><aside class="callout"><strong>Отмена имеет границы</strong><p>Отмена восстанавливает доступные версии файлов. Она не возвращает прежнее состояние памяти Python и не отменяет все последствия уже выполненного кода. Просматривайте изменения до продолжения работы.</p></aside>
<h2 id="limits">Лимиты и расходы</h2><p>На сервере задаются общий лимит вопросов и промежуток между ними. Комната может уменьшить число вопросов и увеличить паузу, но не ослабить серверные ограничения. Пустое значение в правилах комнаты означает настройку сервера; ограничения участников не распространяются на преподавателя.</p><p>При использовании платного API расходы относятся к аккаунту владельца провайдера. Для локального endpoint вычислительные ресурсы предоставляет оператор. Не включайте <code>OPEN_SEMINAR_CREATION=true</code> на публичном сервере без осознанной необходимости: анонимное создание через API может позволить чужим людям расходовать ваш лимит.</p>
<h2 id="council">Разбор Консилиума</h2><p>Преподаватель может попросить оракула разобрать сданные решения. Провайдер получает задание, контекст и примеры решений; имена участников отдельно к запросу не добавляются. Имена или другие данные внутри самого текста решений могут остаться в переданном содержимом.</p><p>Сводка предлагает, что верно, какая ошибка типична и что показать классу. Это помощь преподавателю при разборе, а не автоматическое оценивание.</p>
<h2 id="errors">Если ответа нет</h2><p>Проверьте текст отказа: исчерпанный лимит, пауза между вопросами, выключенный оракул или ошибка провайдера требуют разных действий. Владелец проверяет ключ, адрес, модель и доступ сервера к API. Не вставляйте ключ в чат комнаты для диагностики.</p>
''')
add('council','Провести занятие','Консилиум','Каждый пишет свой ответ; преподаватель выбирает, что обсудить вместе.', '''
<h2 id="prepare">Подготовьте задание</h2><ol><li>Создайте занятие формата «Консилиум» или выберите это состояние замка у отдельной ячейки.</li><li>В общих ячейках подготовьте данные, импорты и условие задачи.</li><li>Выполните подготовительный код, чтобы необходимые значения появились в ядре.</li><li>Откройте ячейку задания: каждый участник получит свой лист.</li></ol><p>Формат «Консилиум» отличается от лекции действием открытия ячейки. Остальные права можно настроить отдельно.</p>
<h2 id="submit">Студенты отвечают отдельно</h2><p>Ответы хранятся на сервере отдельно от общего документа. Студент пишет свой вариант и нажимает «Сдать». До показа преподавателем участник видит собственную работу; преподаватель видит поступившие ответы.</p><p>В меню замка каждой ячейки настройка <strong>«Запуск студентам»</strong> предлагает три режима: <strong>«Только преподаватель»</strong> (по умолчанию), <strong>«Студенты запускают сами»</strong> и <strong>«По запросу преподавателю»</strong>. Общее правило «Запускать код» само не разрешает запуск попыток Консилиума. Во всех режимах попытки проходят последовательно через одно ядро комнаты.</p>
<h2 id="run-requests">Запуск по запросу</h2><p>Студент нажимает <strong>«Попросить запуск»</strong> у своего решения и ждёт решения преподавателя. Сдавать ответ перед запросом не обязательно. Пока запрос ожидает решения, его можно отменить; после отказа — попросить снова. Правка текста снимает прежний запрос: для новой версии нужно отправить новый.</p><p>В пульте преподавателя список <strong>«Запросы на запуск»</strong> показывает ожидающие запросы, включая черновики. Выберите участника, проверьте код и нажмите <strong>«Разрешить запуск»</strong> или <strong>«Отклонить»</strong>. Разрешение ставит проверенную версию в общую очередь; до разрешения код не запускается. Сам запрос не означает сдачу ответа. Смена режима запуска или закрытие Консилиума снимает запросы.</p>
<h2 id="discuss">Разберите ответы с группой</h2><p>В стопке ответов преподаватель переходит между попытками, запускает выбранный код и нажимает <strong>«Показать классу»</strong>. Можно ответить одному автору или группе схожих решений. Сводка помогает выбрать, что обсудить; <a href="oracle.html#council">оракул</a> может предложить разбор, если он настроен.</p><p>Когда фокус находится в стопке: <kbd>←</kbd> / <kbd>→</kbd> переключают попытку, <kbd>Shift</kbd> + стрелка — группу, <kbd>Enter</kbd> показывает ответ классу.</p>
<h2 id="shared">Что именно остаётся общим</h2><aside class="callout"><strong>Личный ответ, общее ядро</strong><p>Попытка видит подготовленные преподавателем <code>df</code>, <code>np</code> и другие значения. Созданные ею имена очищаются по завершении, но изменения объектов и файлов сохраняются. Это не изолированный экзаменационный запуск.</p></aside><p>Например, изменение общей таблицы на месте влияет на следующую попытку. Для учебной задачи предпочтительнее работать с копией:</p>'''+code('''# df подготовлен преподавателем
local_df = df.copy()
local_df = local_df.dropna()
print(local_df.shape)''')+'''<p>Копирование помогает организовать обычную задачу, но не создаёт техническую границу безопасности. Для независимого оценивания, защиты от вмешательства и проверки личности нужна отдельная система.</p>
<h2 id="practice">Удобный сценарий на занятии</h2><p>Начните с короткого вопроса, дайте время на индивидуальный ответ, выберите два разных подхода и покажите их по очереди. Затем верните объяснение в общую тетрадь и сохраните итоговый корректный вариант. Публикация тетради не является выгрузкой всех личных ответов.</p>
''')
add('lectures','Провести занятие','Лекция, проекция и пульт','Показывайте PDF и рукописные заметки на проекторе и в браузерах студентов.', '''
<h2 id="start">Подготовьте лекцию</h2><p>Создайте занятие формата «Лекция»: по умолчанию тетрадью, запуском кода, файлами и общим экраном управляет преподаватель. Загрузите PDF со слайдами в файлы комнаты и откройте его на общем экране.</p><p>Рядом может оставаться ноутбук с примерами. Выбор формата не запрещает практику: преподаватель может открыть отдельную ячейку всей группе или включить Консилиум.</p>
<h2 id="screen">Подключите проектор</h2><p>Откройте проекцию этой же комнаты: <code>/s/ID/screen</code>. Это отдельное представление занятия, а не новая комната. На компьютере проектора выполните вход в нужную комнату. Переключайте слайды стрелками или презентационным пультом.</p><p>Проекция рассчитана на большой экран. Она позволяет показывать документ и примеры из тетради во время объяснения. Заранее проверьте масштаб, полноэкранный режим браузера и связь устройства с сервером.</p>
<h2 id="tablet">Подключите планшет</h2><p>В преподавательском интерфейсе создайте ссылку для пульта и откройте её на планшете. Ссылка-ключ обменивается на вход преподавателя; не отправляйте её студентам. Обычный адрес пульта этой комнаты — <code>/s/ID/pult</code>, но сам по себе он не выдаёт права преподавателя.</p><p>Пульт даёт перо, маркер, ластик и указку. Можно добавить чистый лист для формулы или схемы. Рукописные пометки видит группа; заметки спикера остаются преподавателю.</p>
<h2 id="follow">Как видит лекцию студент</h2><p>Слайды и пометки доступны в браузере студента. Он может отойти от текущей страницы, вернуться к предыдущей, дочитать и затем догнать лекцию. Правило «Показывать документ комнате» управляет общим экраном и не запрещает личный просмотр документов.</p>
<h2 id="after">Материалы после лекции</h2><p>Завершение занятия сохраняет комнату для чтения. Публикация создаёт отдельную страницу с выбранными версиями тетради и выводами, но не переносит в неё автоматически файлы комнаты, PDF или всю работу пульта. Если нужно раздать PDF, организуйте доступ к нему отдельно.</p><p>Перед следующей лекцией убедитесь, что открыли нужную комнату на всех устройствах. Если планшет потерял права, войдите через новую ссылку пульта; не публикуйте административный ключ для восстановления доступа.</p>
''')
add('publishing','Провести занятие','Публикации и курсы','Превратите живую тетрадь в материал для чтения и сохраните одну ссылку на курс.', '''
<h2 id="links">Три адреса, три назначения</h2><div class="table-wrap"><table><thead><tr><th>Ссылка</th><th>Что открывается</th></tr></thead><tbody><tr><td><code>/s/…</code></td><td>Живая комната. Действия зависят от правил и завершения занятия.</td></tr><tr><td><code>/p/…</code></td><td>Отдельная публикация для чтения, без ядра и совместного редактирования.</td></tr><tr><td><code>/c/…</code></td><td>Курс: список опубликованных занятий и будущих тем.</td></tr></tbody></table></div><p>Публикация не закрывает живую комнату. Если работа группы закончена, дополнительно нажмите «Закончить занятие».</p>
<h2 id="publish">Опубликуйте занятие</h2><ol><li>В списке занятий откройте <strong>«Опубликовать…»</strong>.</li><li>Выберите версии тетради, которые должны стать шагами объяснения, и дайте им названия.</li><li>Проверьте код и выводы на данные, которые нельзя раскрывать.</li><li>Нажмите <strong>«Опубликовать»</strong> и откройте полученную страницу как читатель.</li><li>Поделитесь ссылкой <code>/p/…</code>. При необходимости задайте короткое имя адреса.</li></ol><p>Состояние тетради на момент публикации включается всегда. Повторная публикация обновляет материал по той же ссылке; обычные правки живой тетради сами в публикацию не переходят.</p>
<h2 id="included">Что станет публичным</h2><p>Публикуются текст и код ячеек, заметки и выводы выбранных шагов: графики, таблицы, сообщения и трейсбеки. Читатель может скопировать код и скачать тетрадь в <code>.ipynb</code>.</p><p>Авторство действий, вопросы оракулу, история терминала и файлы комнаты в публикацию не включаются. Но имя, ключ или персональные данные, уже напечатанные в коде или выводе, остаются частью опубликованного текста. Проверьте каждый выбранный шаг, а не только итоговый.</p><p>Страница просит поисковые системы не индексировать её. Это не контроль доступа: любой, кто получил ссылку, может открыть материал и передать его дальше.</p>
<h2 id="course">Соберите курс</h2><p>В панели откройте <strong>«Курсы»</strong> и создайте курс. Задайте название и описание, добавьте занятия и расположите их в нужном порядке. Будущие темы можно включить в план до создания комнат, указав название и период словами.</p><p>Дайте группе адрес <code>/c/…</code> один раз. По мере публикации занятий материалы появляются в списке. Страница курса не раскрывает адреса живых комнат: ссылку на текущее занятие выдавайте отдельно.</p>
<h2 id="addresses">Короткие и старые адреса</h2><p>Для публикации или курса можно задать имя из строчных латинских букв, цифр и дефисов длиной от 3 до 64 символов; начало и конец — буква или цифра. Например, <code>/c/python-autumn</code>.</p><p>После переименования прежний адрес продолжает вести к текущему материалу и остаётся занят. Освобождение прежнего имени прекращает работу старой ссылки и позволяет использовать его снова. Освобождайте его только тогда, когда готовы изменить уже розданный адрес.</p>
<h2 id="remove">Снять публикацию или удалить комнату</h2><p>После снятия публикации страница сообщает об этом. Публикация и архивация не изменяют права живой комнаты. Удаление занятия по умолчанию сохраняет опубликованный материал; внимательно прочитайте варианты в подтверждении удаления.</p><p>Публикация удобна для чтения, но не заменяет <a href="backup-restore.html">резервную копию сервера</a>: в ней нет всех рабочих файлов, конфигурации, секретов и данных комнаты.</p>
''')
add('install','Свой сервер','Установка на Linux','Один сервер Linux amd64, точный выпуск, отдельный runtime для каждой комнаты.', '''
<h2 id="requirements">Что подготовить</h2><p>Производственная установка рассчитана на одну Linux amd64 VM с systemd и IPv4. Нужны root-доступ, curl, Python 3, iptables и стандартные Linux-утилиты. Node.js и Docker daemon на сервер устанавливать не требуется: приложение и ядра работают через k3s/containerd.</p><p>Выделите ресурсы для k3s, приложения и каждой одновременно работающей комнаты. Число подключённых студентов само по себе не определяет нагрузку: важны код, память, файлы и число активных ядер. См. <a href="environments.html#capacity">планирование ресурсов</a>.</p><aside class="callout"><strong>Выберите конкретный выпуск</strong><p>Для исходников нужен доступ к закрытому репозиторию. Для установки можно получить от сопровождающего <code>release.json</code>, соответствующий ему <code>colloq-deploy.tar.gz</code> и <code>SHA256SUMS</code>. Здесь нет универсальной ссылки на «последнюю версию»; не подставляйте случайные образы или версии k3s.</p></aside>
<h2 id="release">Проверьте комплект выпуска</h2><p>Получите артефакты через доверенный канал. Проверьте контрольные суммы, распакуйте архив инструментов и работайте из его каталога. Манифест фиксирует исходный коммит, реальные digest образов приложения, брокера и окружений, точную версию k3s и хеши инструментов.</p>'''+code('''sha256sum -c SHA256SUMS
tar -xzf colloq-deploy.tar.gz
python3 scripts/release.py validate --release release.json
sudo scripts/cluster.sh validate --release release.json
scripts/cluster.sh render --release release.json > resources.json''')+'''<p>Контрольная сумма обнаруживает повреждение, но не доказывает доверие к отправителю. Инструменты должны соответствовать выбранному выпуску: локально изменённые или несовпадающие файлы установщик отвергает.</p>
<h2 id="config">Подготовьте конфигурацию</h2><p>Создайте приватный файл <code>instance.env</code>. Полный перечень переменных есть в <code>.env.example</code> исходного комплекта. Минимальный пример для собственного домена:</p>'''+code('''PUBLIC_URL=https://seminar.example.edu
INSTITUTION=Учебная лаборатория
ADMIN_EMAIL=teacher@example.edu
TZ=Europe/Moscow
OPEN_SEMINAR_CREATION=false
MAX_UPLOAD_MB=50
MAX_SESSION_MB=1024''')+'''<p>Значения читаются как данные, не как shell-скрипт. В приложение попадает только разрешённый список переменных. Runtime-ключи генерирует и монтирует установщик; не вставляйте их в ячейки или браузер.</p>
<h2 id="install">Установите и проверьте</h2>'''+code('''sudo scripts/cluster.sh install --release release.json \\
  --env-file /path/to/instance.env
sudo scripts/cluster.sh status
sudo scripts/cluster.sh smoke''')+'''<p>Если образы приватные, добавьте к <code>install</code> аргумент <code>--registry-config /path/to/pull-only-config.json</code>. Используйте отдельные учётные данные только для загрузки нужных образов, а не основной ключ GitHub или Vast.</p><p>Установщик отвергает чужую существующую установку k3s, несовпадающую версию k3s и старый <code>colloq.service</code>. Миграцию старого сервера проводите отдельно: сохраните исходную машину, выгрузите данные подходящими ей инструментами и восстановите совместимую копию в подготовленную установку.</p>
<h2 id="address">Откройте внешний адрес</h2><p>Приложение доступно host-прокси по <code>127.0.0.1:30080</code>. Настройте <a href="networking.html">HTTPS через Caddy или relay</a>. Порты Kubernetes, брокера и Jupyter остаются приватными.</p><p>После публикации адреса откройте <code>/admin</code>, завершите настройку владельца и выполните <a href="getting-started.html">первое занятие</a>. Проверяйте ссылку с устройства студента.</p>
<h2 id="acceptance">Проверка перед реальной группой</h2><ul><li>Создайте две комнаты и выполните в каждой Python-код; убедитесь в разделении переменных, файлов и Jupyter-токенов.</li><li>Проверьте фактическую сетевую политику, в том числе обращения комнаты к локальному узлу.</li><li>Перезапустите приложение и брокер и проверьте восстановление подключения к ядрам.</li><li>Пройдите цикл резервной копии и восстановления, затем обновление и откат выбранных выпусков.</li><li>Для GPU выполните реальную CUDA-операцию; HTTP health недостаточно.</li></ul><p>Локальные тесты проверяют код и отказы, но не доказывают корректность конкретной VM. Ограничения защиты описаны в <a href="security.html">модели изоляции</a>.</p>
''')
add('language','Свой сервер','Язык интерфейса','Выберите русский или английский для всего сервера Colloq.', '''
<h2 id="choose">Выбор в панели</h2><ol><li>Войдите в <code>/admin</code> как владелец сервера.</li><li>Откройте меню со значком глобуса и текущим языком. На телефоне рядом со значком показано <strong>RU</strong> или <strong>EN</strong>.</li><li>Выберите <strong>Русский</strong> или <strong>English</strong>. Текущий язык отмечен галочкой. Меню напоминает: <strong>«Изменится у всех участников.»</strong></li><li>Дождитесь подтверждения сервера: интерфейс переключится, а меню закроется. Если подтвердить изменение не удалось, меню останется открытым с сообщением и кнопкой <strong>«Повторить»</strong>.</li></ol><p>Настройка общая для всего сервера: панели преподавателя, экранов входа, учебных комнат, пульта и страниц опубликованных материалов. Менять её может только владелец; у преподавателей и студентов отдельного выбора языка нет.</p>
<h2 id="live">Открытые занятия</h2><p>Подключённые комнаты получают новый язык без перезагрузки. Код, положение курсора и несохранённые поля форм остаются на месте. Остальные открытые страницы приложения проверяют настройку при возвращении на вкладку и каждые 30 секунд. Если связи нет, браузер использует последний известный язык.</p>
<h2 id="content">Что переводится</h2><p>Переключаются подписи, кнопки, подсказки, новые сообщения Colloq, даты и счётчики. Названия занятий, имена участников, код, файлы, ответы студентов и переписка сохраняются в исходном виде. Вывод Python и сообщения внешних сервисов также остаются такими, какими их вернул источник.</p><p>Оракул получает выбранный язык как язык ответа по умолчанию; в самом вопросе можно попросить другой. Уже полученные ответы, результаты проверок и записи истории не переписываются. Статическая выгрузка опубликованных материалов сохраняет язык, выбранный при её сборке; для смены языка её нужно выгрузить и опубликовать заново. Публичный сайт <code>colloq.ru</code> и эта документация имеют собственный язык и не зависят от настройки вашего сервера.</p>
<h2 id="default">Начальная настройка сервера</h2><p>По умолчанию используется русский. До первого выбора в панели можно задать <code>UI_LANGUAGE=ru</code> или <code>UI_LANGUAGE=en</code> в конфигурации приложения. После изменения переменной перезапустите приложение.</p><p>Выбор владельца хранится в базе данных и имеет приоритет над <code>UI_LANGUAGE</code>. Он переживает перезапуск, обновление и восстановление базы из резервной копии. Это не настройка отдельной комнаты или браузера.</p>
''')
add('environments','Свой сервер','Окружения и ресурсы','Подготовьте библиотеки заранее и закрепите образ Python за занятием.', '''
<h2 id="catalog">Окружение в production</h2><p>Окружение — запись каталога с именем и immutable digest образа ядра. Занятие сохраняет выбранную ревизию. Изменение окружения по умолчанию или выпуск нового образа не меняют Python уже созданного занятия.</p><p>Панель <strong>«Окружения»</strong> показывает доступные ревизии и позволяет выбрать окружение по умолчанию для новых занятий. Если закреплённая ревизия недоступна, запуск завершается ошибкой; сервер не заменяет её случайным текущим образом.</p>
<h2 id="build">Добавьте библиотеки</h2><ol><li>Измените требования окружения в <code>kernel/environments/</code>. Директива <code># colloq: from NAME</code> задаёт наследование, <code># colloq: python 3.12</code> — версию Python: от 3.10 до 3.13, по умолчанию — версия из <code>ARG PARENT</code> в <code>kernel/Dockerfile</code>. Версию задаёт корень цепочки наследования: слой поверх готового образа ставит колёса под тот интерпретатор, который пришёл из базы, и заменить его не может, поэтому окружение с <code>from</code>, просящее другую версию, к сборке не принимается. Для pip обе строки — комментарии.</li><li>Зафиксируйте исходный коммит и подготовьте новый выпуск на доверенной машине сборки или через release CI.</li><li>Опубликуйте образы и каталог с реальными digest.</li><li>Установите выпуск и проверьте импорт и типовую задачу из занятия.</li></ol><p>Production не собирает образ из веб-запроса и не получает Docker socket. Описание пакетов отражает запрошенные зависимости, а не полный pip lock: повторная сборка диапазонов версий может дать другой образ. Установленный digest при этом остаётся неизменным.</p>
<h2 id="release-build">Сборка выпуска сопровождающим</h2><p>Workflow <strong>Publish immutable release</strong> принимает тег версии и точный проверенный patch k3s, собирает выбранные окружения и прикладывает манифест, архив инструментов и суммы. Альтернатива — локальный <code>scripts/release-build.py</code>. Пример со значениями, которые нужно заменить:</p>'''+code('''python3 scripts/release-build.py --version YOUR_VERSION_TAG \\
  --registry YOUR_REGISTRY_PREFIX --source-commit FULL_COMMIT_SHA \\
  --k3s-version EXACT_TESTED_K3S_VERSION --environments base,cv''')+'''<p>Для GPU дополнительно укажите <code>--gpu-toolkit-version EXACT_NVIDIA_APT_VERSION</code> и <code>--gpu-device-plugin-image NVIDIA_PLUGIN_IMAGE_AT_SHA256</code>. Авторизуйтесь в registry на машине сборки. Публикация артефактов сама по себе не является проверкой на целевой VM.</p>
<h2 id="capacity">Ресурсы комнаты</h2><p>Брокер по умолчанию задаёт комнате <code>2Gi</code> памяти, <code>2</code> CPU и <code>2Gi</code> ephemeral storage; requests равны limits. Это пределы ресурсов, а не обещание определённого числа студентов или скорости обучения модели.</p><p>Память <code>/dev/shm</code> входит в лимит Pod: по умолчанию 64 MiB у CPU-окружения и 1 GiB у GPU. Установщик задаёт лимит 256 PID на Pod. Namespace ResourceQuota и дополнительные правила допуска нагрузки оператор настраивает отдельно; готовой ResourceQuota в комплекте нет. Для workspace нет принудительной покомнатной файловой квоты; следите за отдельным файловым разделом и свободным местом.</p>
<h2 id="gpu">GPU-окружения</h2><p>Каждая GPU-комната запрашивает один <code>nvidia.com/gpu</code> и <code>RuntimeClass nvidia</code>. Совместное использование устройства не включается автоматически. Несколько одновременных GPU-комнат требуют соответствующего числа доступных устройств либо отдельно спроектированной и проверенной политики sharing.</p><p>Установщик сохраняет драйвер хоста, ставит закреплённые NVIDIA Container Toolkit и device plugin и проверяет реальную PyTorch CUDA-операцию. Для проверки нужен свободный GPU; занятая карта не отбирается у действующего занятия.</p>'''+code('''sudo scripts/cluster.sh gpu-preflight
sudo k3s kubectl get nodes \\
  -o "custom-columns=NAME:.metadata.name,GPU:.status.allocatable.nvidia\\.com/gpu"''')+'''<p>Обычная доступность приложения не доказывает GPU-готовность. Если карта не выделяется или CUDA не работает, разберите причину до начала занятия.</p>
<h2 id="retention">Сохраняйте старые образы</h2><p>Исторические ревизии каталога сохраняются, пока на них ссылаются комнаты. Не удаляйте соответствующие образы из registry. Резервная копия приложения содержит каталог и digest, но не слои образов.</p>
''')
add('networking','Свой сервер','Домен и доступ из аудитории','Выберите способ публикации сервера и проверьте настоящую студенческую ссылку.', '''
<h2 id="choose">Выбор транспорта</h2><p>Команды <code>make host</code>, <code>host-direct</code> и настройки relay ниже выполняются из полного исходного checkout соответствующей версии. Минимальный архив <code>colloq-deploy.tar.gz</code> не содержит Makefile и всех host-инструментов. Если у вас только архив установки, настройте собственный HTTPS reverse proxy к <code>127.0.0.1:30080</code> или получите исходный комплект у владельца.</p><div class="table-wrap"><table><thead><tr><th>Ситуация</th><th>Способ</th><th>Что нужно</th></tr></thead><tbody><tr><td>Свой публичный IP и порты 80/443</td><td>Прямой Caddy</td><td>Домен, root и DNS-настройка.</td></tr><tr><td>Ноутбук или VM за NAT</td><td>Свой relay с FRP</td><td>Публичный relay с Caddy/frps, исходящее соединение.</td></tr><tr><td>Короткая демонстрация</td><td>Quick Cloudflare tunnel</td><td>Доступность Cloudflare из сети аудитории; URL меняется.</td></tr><tr><td>Постоянный адрес через Cloudflare</td><td>Named tunnel</td><td>Предварительная настройка tunnel и DNS.</td></tr></tbody></table></div><p>Проверяйте доступность выбранного транспорта из реальной сети студентов. Cloudflare tunnel может быть недоступен из части российских сетей; постоянное доменное имя само по себе этот маршрут не меняет.</p>
<h2 id="direct">Прямой публичный сервер</h2><p>Если у машины действительно есть публичный адрес и доступные TCP 80/443, host-инструменты могут настроить Caddy:</p>'''+code('''sudo make host-direct HOST=seminar.example.edu''')+'''<p>Подготовьте <code>CF_TOKEN</code> и <code>CF_ZONE</code> для используемой инструментами Cloudflare DNS-настройки. Эти полномочия нужны оператору, не приложению. Caddy работает на хосте как служба и проксирует production на <code>127.0.0.1:30080</code>.</p>
<h2 id="relay">Свой relay</h2><p>На отдельной публичной машине один раз настройте Caddy и FRP:</p>'''+code('''make relay-setup WHERE=root@your-relay
make host HOST=seminar.example.edu''')+'''<p>После <code>relay-setup</code> перенесите выданные параметры <code>RELAY_DOMAIN</code>, <code>RELAY_ADDR</code>, <code>RELAY_PORT</code> и <code>RELAY_TOKEN</code> в операторскую конфигурацию instance. Команда <code>make host</code> выполняется на машине Colloq; она подключается к relay наружу. Для Vast используется этот же транспорт.</p><p>Код продолжает исполняться на машине Colloq. Relay обслуживает трафик и становится общей точкой отказа для всех проходящих через него занятий. Следите за его пропускной способностью и ресурсами; размер аудитории проверяйте нагрузочным сценарием.</p><p>Собранный фронтенд relay раздаёт сам. <code>make host</code> выкладывает на него <code>assets/</code>, <code>fonts/</code> и <code>pdf/</code> из сборки; через туннель остаётся ходить только живой комнате. Отсутствующий или устаревший файл relay пропускает в туннель, поэтому зеркало не ломает занятие. Проверка: <code>curl -sI https://seminar.example.edu/assets/&lt;файл&gt;</code> — ответ с <code>X-Colloq-Mirror: hit</code> пришёл от relay. Имена кусков прежней сборки остаются доступными 30 дней.</p>
<h2 id="cloudflare">Cloudflare tunnel</h2>'''+code('''# Временный адрес
make host

# Именованный tunnel: сначала настройка, затем запуск
make tunnel-setup HOST=seminar.example.edu
make host HOST=seminar.example.edu''')+'''<p>Quick tunnel выдаёт случайный адрес и живёт вместе с процессом. Скрипт сначала получает URL, затем обновляет <code>PUBLIC_URL</code> и перезапускает приложение. Напечатанная ссылка <code>/admin/t/…</code> предназначена только владельцу.</p>
<h2 id="url">Корректные ссылки</h2><p><code>PUBLIC_URL</code> — внешний origin сервера, из которого строятся ссылки. При изменении адреса установленного k3s-instance используйте:</p>'''+code('''sudo scripts/cluster.sh public-url https://seminar.example.edu''')+'''<p>Не оставляйте <code>localhost</code> во внешних ссылках. После смены origin браузер может потребовать повторного преподавательского входа. Перед занятием проверьте HTTPS, вход студента, совместную правку, запуск ячейки и открытие файла с другого устройства.</p>
<h2 id="private">Какие порты не публиковать</h2><p>Публичный вход должен вести к приложению через HTTPS. Не публикуйте runtime broker <code>8787</code>, Jupyter <code>8888</code>, Kubernetes API <code>6443</code>, kubelet <code>10250</code> и Flannel <code>8472</code>. NodePort <code>30080</code> ограничен loopback для host-прокси.</p><p>Firewall, защищающий публичные порты, не решает все обращения из Pod к локальному узлу. Прочитайте <a href="security.html#network">ограничение NetworkPolicy</a> перед допуском недоверенного кода.</p>
''')
add('backup-restore','Свой сервер','Резервные копии и восстановление','Сохраните данные, рабочие файлы и ключи вне сервера — и проверьте обратный путь.', '''
<h2 id="contents">Что хранится на диске</h2><p>Стандартный k3s-instance использует <code>/var/lib/colloq/data</code> и <code>/var/lib/colloq/workspace</code>. Рядом находятся приватная конфигурация, постоянные секреты и сведения о выпусках. Local PV переживают замену Pod, но не потерю диска или удаление VM.</p><p>Переносимый архив включает SQLite, workspace, конфигурацию, ключи приложения и runtime, release/catalog и контрольные суммы. Он содержит секреты: храните его приватно и вне исходной машины. Слои контейнерных образов в архив не входят, поэтому registry должен сохранять нужные digest.</p>
<h2 id="modes">Выберите режим копии</h2><div class="table-wrap"><table><thead><tr><th>Режим</th><th>Поведение</th><th>Цена</th></tr></thead><tbody><tr><td><code>consistent</code></td><td>Останавливает приложение, брокер и все ядра до копирования.</td><td>Перерыв в работе; память Python теряется.</td></tr><tr><td><code>live</code></td><td>Снимок SQLite и независимое копирование изменяющихся файлов.</td><td>Не атомарный снимок базы и workspace; меняющиеся файлы могут сорвать копирование.</td></tr></tbody></table></div>'''+code('''sudo env MODE=consistent scripts/backup.sh
# После проверки копии возобновите работу явно:
sudo scripts/cluster.sh start

# Либо копирование во время работы:
sudo env MODE=live scripts/backup.sh''')+'''<p>Consistent-копия по умолчанию оставляет запись остановленной. При ошибке копирования сначала выясните причину; не запускайте работу автоматически. Для удалённой Vast VM используйте <a href="vast.html#backup">vast-sync</a>.</p>
<h2 id="restore">Восстановление на Linux</h2><p>Используйте доверенный архив, соответствующее имя окружения и явно совместимый выпуск. На подготовленном целевом сервере выполните из каталога его инструментов:</p>'''+code('''sudo scripts/cluster.sh prepare --release /path/release.json
sudo bash scripts/restore.sh \\
  --archive /path/colloq-consistent.tar.gz \\
  --release /path/release.json --replace
sudo scripts/cluster.sh prepare --release /path/release.json
sudo scripts/cluster.sh start''')+'''<p>Пример выше соответствует копии без заданного <code>NAME</code>. Для именованной копии добавьте такое же имя при восстановлении, например <code>sudo env NAME=hse bash scripts/restore.sh …</code>. Имя должно совпадать с архивом. <code>--replace</code> явно разрешает замену существующих деревьев; даже начальный <code>prepare</code> создаёт секреты и конфигурацию. Инструменты сначала проверяют архив и совместимость, останавливают всех писателей, затем устанавливают целые деревья. Предыдущие деревья сохраняются приватно в <code>replaced-…</code>; не удаляйте их до проверки восстановления.</p><p>Повторный <code>prepare</code> согласует восстановленные runtime-секреты и старые ревизии каталога. На этом шаге не передавайте новый <code>--env-file</code>, если не собираетесь сознательно заменить восстановленную конфигурацию.</p>
<h2 id="verify">Проверьте результат</h2><ul><li>Открываются ожидаемые комнаты и старые ссылки преподавателей и студентов.</li><li>Тетради, публикации, данные и файлы соответствуют выбранной копии.</li><li>Две комнаты исполняются независимо, а ядра используют нужные ревизии.</li><li>В GPU-окружении проходит реальная CUDA-операция.</li><li>С исходной машины можно восстановиться даже после потери её диска.</li></ul><p>Проверка контрольных сумм и SQLite <code>quick_check</code> обязательна для целостности, но не заменяет проверку материалов и рабочего сценария.</p>
<h2 id="interrupted">Если восстановление прервалось</h2><p>Незавершённая операция оставляет <code>.restore-in-progress</code>. Пока она не завершена, запуск и изменение deployment блокируются. Повторите восстановление тем же проверенным архивом с <code>--recover --replace</code>, либо, при работе из полного checkout, через make с <code>RECOVER=1 REPLACE=1</code>. Не удаляйте маркер вручную, чтобы обойти защиту.</p><p>Явное восстановление может вернуть удалённые комнаты: оно согласует файлы с постоянными runtime-резервациями удалённых ID. Обычные start/stop/update эти резервации не сбрасывают.</p>
<h2 id="legacy">Старые форматы и k3s</h2><p><code>make restore-legacy DB=old.db FILES=old-files.tar.gz</code> предназначен для старого локального формата и отвергается поверх установленного кластера. Миграция старого сервиса требует отдельной проверки схемы и каталога.</p><p>Backup Colloq не является backup datastore k3s. Перед изменением версии k3s сохраните его datastore и server token отдельно по документации k3s.</p>
''')
add('updates','Свой сервер','Обновление и откат','Обновляйте в окно обслуживания и сохраняйте совместимый путь назад.', '''
<h2 id="before">До обновления</h2><ol><li>Получите выбранный выпуск и соответствующие ему инструменты; проверьте суммы и манифест.</li><li>Убедитесь, что образы доступны с используемыми registry-правами, а на диске хватает места для backup.</li><li>Проверьте заявленную совместимость схемы данных и сохраните внешний backup текущего состояния.</li><li>Согласуйте перерыв: приложение, брокер и ядра будут заменены.</li></ol><p>Используйте инструменты выбранного выпуска. Установка не подменяет отсутствующий digest и не принимает локально исправленный архив инструментов.</p>
<h2 id="update">Выполните обновление</h2>'''+code('''sudo scripts/cluster.sh update --release next-release.json
sudo scripts/cluster.sh status
sudo scripts/cluster.sh smoke''')+'''<p>Preflight проверяет реальную загрузку образов и non-root execution до остановки писателей. Update проверяет совместимость и делает согласованную переносимую копию, затем останавливает приложение и runtime-нагрузки. Тетради и файлы остаются; переменные Python теряются.</p><p>После обновления проверьте старую комнату, новую комнату, публикации, файлы и исполнение. Убедитесь, что старые занятия продолжают использовать закреплённые ревизии окружений.</p>
<h2 id="rollback">Когда нужен откат</h2>'''+code('''sudo scripts/cluster.sh rollback --release previous-release.json
sudo scripts/cluster.sh status
sudo scripts/cluster.sh smoke''')+'''<p>Rollback также проверяет совместимость, создаёт consistent-backup и требует перерыва. Возврат старого образа не отменяет несовместимую миграцию базы: в таком случае нужен подходящий backup и процедура <a href="backup-restore.html">восстановления</a>.</p><p>При неудачной readiness-проверке желаемый выпуск остаётся записанным, а предыдущий находится в <code>releases/previous.json</code>. Оператор выбирает откат явно; автоматического непроверенного понижения схемы нет.</p>
<h2 id="k3s">Версия k3s меняется отдельно</h2><p>Colloq-выпуск требует точную версию k3s. Её изменение — самостоятельная операция: backup datastore и server token, план обновления k3s, затем Colloq-выпуск, совместимый с новой точной версией. Обычный update приложения не обещает безопасного upgrade кластера.</p>
<h2 id="old-images">Старые комнаты и образы</h2><p>Сохраняйте registry-образы, на которые ссылаются старые комнаты и резервные копии. Даже если окружение исчезло из нового выпуска, каталог сохраняет нужные прежние ревизии. Очистка registry должна учитывать эти ссылки.</p><p>Одна VM и локальные тома предполагают обслуживание с перерывом. Этот процесс не обеспечивает HA или обновление без остановки.</p>
''')
add('vast','Свой сервер','Развёртывание на Vast VM','Арендуйте полную VM, установите точный выпуск и проверяйте GPU выполнением кода.', '''
<h2 id="prepare">Подготовка до аренды</h2><p>Рабочий сценарий использует полную <strong>Vast VM</strong> с systemd, а не обычный Docker instance. VM содержит один узел k3s. Проверьте доступность VM-предложений, рабочий NVIDIA-драйвер, зарегистрированный SSH-ключ, цену и дисковое пространство.</p><p>На доверенной операторской машине нужны манифест выпуска и checkout с его точным <code>sourceCommit</code>. <code>vast-up</code> упаковывает инструменты именно из этого коммита. Приложение не получает ключ аккаунта Vast.</p><p>В конфигурации подготовьте <code>VAST_TOKEN</code>, SSH-ключ и параметры существующего <a href="networking.html#relay">relay</a>. Для приватных образов задайте <code>VAST_REGISTRY_CONFIG</code> с отдельными pull-only правами и <code>VAST_REGISTRY_READ_ONLY=1</code>. Файл должен содержать только Docker <code>auths</code>, без credential helpers. Синтаксическая проверка не доказывает действительность прав — загрузку проверяет preflight.</p>
<h2 id="launch">Запуск и наблюдение</h2><p>Для выпуска, чьи образы действительно доступны анонимно:</p>'''+code('''VAST_PUBLIC_IMAGES=1 RELEASE=/absolute/path/release.json \\
  NAME=hse HOST=hse.example.edu make vast-up
NAME=hse make vast-status
NAME=hse SINCE=2h make vast-logs''')+'''<p>Замените имя, домен и путь. <code>VAST_PUBLIC_IMAGES=1</code> — явное утверждение о доступности образов, а не способ сделать приватные образы публичными. При смешанных registry оно относится к образам без переданных учётных данных.</p><p>Инструмент проверяет манифест до обращения к аккаунту, предлагает VM и требует подтверждения цены. Передаются только инструменты выпуска и разрешённая конфигурация. При обновлении удалённая конфигурация сохраняется: изменение каждого поля локального <code>.env</code> не обязано менять VM.</p>
<h2 id="gpu">Критерий готовности GPU</h2><p>HTTP-ответ приложения и вывод <code>nvidia-smi</code> недостаточны. Установщик проверяет RuntimeClass, allocatable GPU и исполняет реальную PyTorch CUDA-операцию в одноразовом non-root Pod. <code>vast-up</code> также запускает cluster smoke с независимыми комнатами.</p><p>Одна GPU-комната запрашивает одно устройство; автоматического sharing или перехода на общее ядро нет. Для GPU-проверки нужна свободная карта. Успешные локальные тесты репозитория не подтверждают работу конкретной аренды.</p>
<h2 id="verified">Проверенная конфигурация</h2><p>9 сентября 2026 года проверили отдельную Vast VM с RTX 3090 (24 ГБ), Ubuntu 22.04, драйвером NVIDIA 580.95.05 и k3s v1.36.4+k3s1. На GPU-занятии два клиента получили результат матричного умножения на PyTorch 2.14.0 / CUDA 13.0 через общий ноутбук.</p><p>Также прошли проверку разделение комнат, сохранение GPU-тензора при перезапуске приложения и брокера, обновление и восстановление удалённого занятия из consistent-копии. Использовались NVIDIA Container Toolkit 1.20.0-1 и device plugin 0.19.3. Тестовая VM удалена после проверки.</p><p>Это результат для конкретной конфигурации. Другие GPU, внешние registry, публичный relay и ограничения доступа к службам узла требуют своей проверки. Обновление выпуска и восстановление копии останавливают ядра; сохранение Python-памяти при этих операциях не обещается.</p>
<h2 id="backup">Скопируйте данные с VM</h2>'''+code('''# Во время работы: база и файлы не составляют атомарный снимок
NAME=hse MODE=live make vast-sync

# Остановить всех писателей и получить согласованную копию
NAME=hse MODE=consistent make vast-sync

# Явно разрешить возобновление после успешной копии
NAME=hse MODE=consistent RESUME=1 make vast-sync''')+'''<p>Архивы сохраняются в <code>backups/hse/</code> на операторской машине. Архив проверяется на VM и после передачи. Он содержит секреты, конфигурацию, release/catalog и workspace; образы остаются в registry.</p><p>Данные Colloq находятся на диске VM, и удаление аренды может их уничтожить. Не рассчитывайте на автоматическое сохранение local PV или возможность подключить отдельный Vast Volume к VM: проверяйте ограничения выбранного продукта у провайдера.</p>
<h2 id="restore">Восстановление и обновление</h2><p>Команды <code>make vast-…</code> выполняются из полного исходного checkout на операторской машине. Для восстановления используйте <a href="backup-restore.html#restore">процедуру portable restore</a> с правильным именем и совместимым выпуском. После восстановления повторите prepare и проверки комнат. Обновление требует остановки ядер; версию k3s меняйте отдельной операцией.</p><p>Старые root-service/Compose установки и неуправляемые непустые данные не мигрируют автоматически. Сохраните старую машину, выгрузите её данные подходящими ей инструментами и проверьте перенос отдельно.</p>
<h2 id="destroy">Перед удалением аренды</h2><p>Получите согласованную копию вне VM, проверьте её и убедитесь, что нужные образы доступны. Только затем выполняйте <code>NAME=hse make vast-down</code> и подтверждайте удаление указанной аренды. Остановленный кластер не является резервной копией.</p><p>Для диагностики используйте длительности <code>SINCE=30m</code> или <code>SINCE=2h</code>. Логи уже удалённых room Pod могут быть недоступны; вывод приложений и ячеек может содержать учебные данные.</p>
''')
add('development','Свой сервер','Локальная разработка','Запустите Colloq на рабочей машине с отдельным Docker-ядром для каждой комнаты.', '''
<h2 id="requirements">Что нужно</h2><p>Получите доступ к закрытому репозиторию у владельца и скачайте исходный код. Установите Docker с работающим daemon и инструменты, указанные в репозитории. Для host-разработки нужен Node.js, совместимый с проектом, npm и зависимости из lockfile. Production-процедура через k3s описана <a href="install.html">отдельно</a>.</p><p>Перед запуском изучите <code>.env.example</code>. Пример ограничивает локальную публикацию адресом <code>127.0.0.1</code>. Без <code>BIND_ADDR</code> development-сервер и Compose-порт по умолчанию доступны на всех интерфейсах, включая Wi-Fi.</p>
<h2 id="docker">Быстрый запуск в Docker</h2>'''+code('''make up''')+'''<p>Команда создаёт локальную конфигурацию, собирает образ ядра и запускает приложение. Каждая комната получает собственный контейнер; общего instance-wide Jupyter нет. Откройте приложение на <code>http://localhost:3000</code> и завершите настройку владельца.</p><p>Данные и файлы находятся в локальных <code>data/</code> и <code>workspace/</code>. Development backend имеет доступ к Docker socket; используйте его для доверенной локальной разработки, а не как production-границу безопасности.</p>
<h2 id="host">Приложение на хосте</h2>'''+code('''npm ci
make dev
NODE_ENV=development KERNEL_BACKEND=docker npm run dev''')+'''<p><code>make dev</code> готовит образ комнатного ядра; приложение запускается следующей командой. Для native macOS дополнительно требуется явное разрешение локального режима файлов:</p>'''+code('''COLLOQ_UNSAFE_DEV_FILES=1 NODE_ENV=development \\
  KERNEL_BACKEND=docker npm run dev''')+'''<p>Этот opt-in не предоставляет защищённую Linux-работу с файлами через <code>/proc/self/fd</code> и запрещён в production. Для сборки и запуска host-сервера есть <code>make run</code>.</p>
<h2 id="envs">Development-окружения</h2>'''+code('''make env-list
make env-new NAME=nlp
# Измените требования в kernel/environments/
make env-build NAME=nlp
make env-use NAME=nlp''')+'''<p>Это команды Docker-разработки. В production окружения публикуются заранее и выбираются из digest-каталога, как описано в <a href="environments.html">руководстве окружений</a>.</p>
<h2 id="translations">Тексты интерфейса</h2><p>Парные переводы находятся в <code>shared/locales/</code>: <code>admin.ts</code>, <code>room.ts</code>, <code>server.ts</code> и <code>common.ts</code>. Добавляйте русский и английский варианты под одним ключом, а в месте показа вызывайте <code>tr(key, params)</code> из <code>shared/i18n.ts</code>. Подставляйте имена и другие переменные параметрами; для счётчиков используйте формы множественного числа, для дат и чисел — общие функции форматирования.</p><p>В Svelte перевод должен вычисляться реактивно при показе: перевод, сохранённый в константу при загрузке модуля, не обновится. Для нативных элементов редактора есть подписка на смену языка; пересоздавать весь редактор ради подписи не нужно. Служебные значения протокола, имена файлов и пользовательские тексты через переводчик не пропускаются. Проверка <code>tests/i18n.test.mts</code> сверяет наличие обоих языков и совпадение параметров; поведение переключателя проверяют тесты <code>instance-language</code>, <code>admin-language</code> и <code>language-room</code>.</p>
<h2 id="checks">Проверки изменений</h2>'''+code('''npm run typecheck
npm test
npm run build''')+'''<p>Запускайте также относящиеся к изменению интеграционные сценарии из <code>tests/README.md</code>. Локальный green test не заменяет deployment smoke, сетевые проверки, backup/restore и реальную GPU-операцию на целевой VM.</p><p>Если ядро не запускается, проверьте Docker daemon, наличие образа, права на socket и пути workspace. Не подставляйте один общий Jupyter endpoint как временную замену отдельным комнатам.</p>
''')
add('security','Свой сервер','Изоляция и ограничения','Какие границы обеспечивает Colloq и что должен проверить оператор.', '''
<h2 id="architecture">Границы компонентов</h2><div class="architecture" aria-label="Браузеры обращаются к приложению через HTTPS. Приложение обращается к приватному runtime-брокеру. Брокер создаёт отдельные Jupyter Pod для комнат."><span>Браузеры</span><b aria-hidden="true">↓ HTTPS</b><span>Приложение Colloq</span><b aria-hidden="true">↓ приватный запрос</b><span>Runtime broker</span><b aria-hidden="true">↓ фиксированный шаблон</b><div><span>Ядро комнаты A</span><span>Ядро комнаты B</span></div></div><p>В production приложение работает с UID 1000 без Docker socket и Kubernetes credentials. Только приватный runtime broker получает namespaced service-account token и создаёт Pod/Service из фиксированного шаблона и доверенного каталога. API не принимает произвольный Pod template или путь хоста.</p><p>Каждая комната получает отдельный Jupyter Pod, токен и workspace subPath. Если runtime, образ или политика недоступны, запуск не происходит: перехода к общему ядру сервера нет.</p>
<h2 id="pods">Ограничения Pod</h2><p>Room Pod работает с UID/GID 1000, без дополнительных capabilities и повышения привилегий, с RuntimeDefault seccomp и read-only корневой файловой системой. Service-account token не монтируется. Записываемые области: каталог комнаты, <code>/tmp</code>, <code>/home/runner</code> и <code>/dev/shm</code>.</p><p>Pod Security требует restricted-профиль. RBAC брокера ограничен операциями с Pod и Service в namespace, но не умеет ограничить каждое поле создаваемого Pod. Поэтому код брокера, его шаблоны и каталог входят в доверенную часть системы.</p>
<h2 id="network">Сеть: проверяйте фактический результат</h2><p>NetworkPolicy разрешает вход к Jupyter только приложению и брокеру. Исходящий трафик комнат по умолчанию разрешён только к DNS Pod в kube-system по TCP/UDP 53. Для загрузки внешних данных нужен отдельно спроектированный outbound proxy или профиль.</p><aside class="callout"><strong>Исключение локального узла</strong><p>Стандартная NetworkPolicy не доказывает блокировку обращения student-кода к сервисам на узле, где исполняется Pod. Включённый host-firewall защищает публичную экспозицию и не закрывает автоматически это исключение.</p></aside><p>Если нужна изоляция от host-сервисов, добавьте CNI с host policy или проверенную firewall-политику с точечным разрешением брокеру доступа к Kubernetes API. До допуска недоверенных пользователей выполните реальный room-to-node connection test. Наличие манифеста не является доказательством.</p>
<h2 id="files">Файлы и квоты</h2><p>Linux production использует доступ через доверенные файловые дескрипторы, <code>O_NOFOLLOW</code> и <code>/proc/self/fd</code>. Символические ссылки не обходят каталог комнаты. Если защищённый механизм недоступен, production отказывает в работе.</p><p>Старый workspace не должен содержать hardlink между комнатами. При миграции копируйте обычные файлы в отдельные каталоги и проверьте владельцев и ссылки. Два имени существующего общего inode не превращаются в независимые данные от проверки пути.</p><p>Размер PVC — метаданные планирования, а не filesystem quota. <code>MAX_UPLOAD_MB</code> и <code>MAX_SESSION_MB</code> ограничивают операции приложения и не сдерживают произвольную запись Python. Используйте отдельный раздел и мониторинг свободного места.</p>
<h2 id="identity">Права, ссылки и секреты</h2><p>Ссылка комнаты даёт доступ к занятию; преподавательская ссылка даёт доступ к панели. Setup-token позволяет владельцу восстановить контроль. Храните секреты и backup приватно; полный доступ к диску сервера означает доступ к данным и ключам.</p><p>Отдельные ответы Консилиума не дают отдельное ядро каждому студенту. Ограничения действий и browser-ban не являются проверкой личности. Автоматического оценивания, SSO и защиты экзаменационного исполнения система не обещает.</p>
<h2 id="limits">Пределы развёртывания</h2><p>Контейнеры разделяют Linux-ядро VM. Это не изоляция виртуальными машинами и не защита от уязвимостей ядра. Одна VM, один узел k3s и local PV не дают HA. Потеря машины требует внешнего backup; обновления прерывают работу и теряют память Python.</p><p>Проверяйте собственную нагрузку. Не существует подтверждённого универсального лимита «сколько студентов выдержит сервер»: результаты зависят от вычислений, вывода, файлов, активных комнат и сети.</p>
''')
add('troubleshooting','Свой сервер','Если что-то не работает','Начните с симптома и проверяйте тот слой, который отвечает за него.', '''
<h2 id="student">Студент не может войти</h2><p>Проверьте, что выдана ссылка <code>/s/…</code>, а не главная страница сервера или административный ключ. Если адрес содержит <code>localhost</code>, исправьте <code>PUBLIC_URL</code>. Проверьте HTTPS и доступ с другой сети, затем состояние relay/tunnel. Доступ преподавателя из своей сети не доказывает доступность из аудитории.</p>
<h2 id="permission">Кнопка недоступна</h2><p>Прочитайте сообщение интерфейса. Проверьте завершение занятия, правила комнаты и замок отдельной ячейки. «Запускать код» ограничивает также файлы и терминал. Преподаватель может продолжить занятие или изменить нужное правило; переустановка сервера здесь не поможет.</p>
<h2 id="kernel">Тетрадь открылась, а Python не запускается</h2>'''+code('''sudo scripts/cluster.sh status
sudo scripts/cluster.sh logs
sudo k3s kubectl -n colloq get pods
sudo k3s kubectl -n colloq get events --sort-by=.lastTimestamp''')+'''<p>Проверьте, работает ли брокер, доступен ли закреплённый image digest, хватает ли CPU/памяти/GPU и корректен ли mount workspace. <code>ImagePullBackOff</code> обычно требует проверки registry и прав, <code>Pending</code> — причин планирования в событиях. Health брокера проверяет доступ к API, а не весь путь исполнения.</p><p>Не заменяйте недоступную ревизию другой без осознанного решения и не подключайте комнаты к общему Jupyter. После устранения причины выполните <code>sudo scripts/cluster.sh smoke</code> и настоящую ячейку через приложение.</p>
<h2 id="python">NameError или изменившиеся данные</h2><p>Возможно, не выполнена подготовительная ячейка, ядро перезапустилось или другой участник изменил общие значения. Проверьте порядок запуска. В Консилиуме изменения объектов и файлов сохраняются между попытками; личные листы не создают личную память Python.</p>
<h2 id="packages">Не устанавливается пакет или не скачиваются данные</h2><p>В production исходящая сеть ядра по умолчанию ограничена DNS, а root filesystem защищён от записи. Подготовьте библиотеку в <a href="environments.html">образе окружения</a>, а данные загрузите в комнату или используйте одобренный оператором outbound-профиль. Не отключайте сетевую защиту всего namespace ради одного импорта.</p>
<h2 id="gpu">CUDA недоступна</h2><p>Проверьте драйвер гостевой VM, NVIDIA RuntimeClass, allocatable devices, наличие свободного устройства и GPU-метаданные каталога. CPU-окружение само не становится GPU-окружением.</p>'''+code('''nvidia-smi
sudo k3s kubectl get runtimeclass
sudo k3s kubectl describe node
sudo scripts/cluster.sh gpu-preflight''')+'''<p>Если GPU занят комнатой, preflight не выселяет её. Повторите проверку в согласованное время со свободной картой. Реальное выполнение CUDA необходимо даже при корректном <code>nvidia-smi</code>.</p>
<h2 id="oracle">Оракул молчит</h2><p>Проверьте выбор оракула при создании, лимит участника, паузу между вопросами и статус завершения занятия. Владелец проверяет provider URL, модель, ключ и доступ приложения к API. Ошибка провайдера не означает поломку Python-ядра. Не отправляйте секреты в диагностический чат.</p>
<h2 id="disk">Заканчивается диск</h2><p>Проверьте раздел с <code>/var/lib/colloq</code> и место под резервные копии. Upload-лимиты приложения не являются квотой для Python. Сначала определите источник роста, сохраните нужные материалы и только затем очищайте данные. Не удаляйте вручную каталог активной комнаты или образы, на которые ссылается каталог.</p>
<h2 id="restore">Запуск заблокирован после restore</h2><p>Проверьте сообщение о <code>.restore-in-progress</code>. Повторите тот же проверенный restore с флагами recovery, описанными в <a href="backup-restore.html#interrupted">руководстве</a>. Снятие маркера вручную может запустить приложение над частично восстановленными данными.</p>
<h2 id="publication">На странице чтения нет файлов или новых правок</h2><p>Это ожидаемо: публикация содержит выбранные версии ячеек и выводов, а не файлы комнаты. После изменения живой тетради выполните повторную публикацию. Если ссылка снята с публикации, вернуть её может преподаватель; права комнаты <code>/s/…</code> от этого не меняются.</p>
<h2 id="report">Что приложить к сообщению о проблеме</h2><p>Укажите версию выпуска, время, шаги воспроизведения, ожидаемый и фактический результат. Добавьте статус компонентов и относящиеся к сбою события. Уберите административные ссылки, ключи, персональные данные и ненужный вывод ячеек. На Vast используйте <code>NAME=hse SINCE=30m make vast-logs</code>; логи удалённых Pod могут уже отсутствовать.</p>
''')
logo='''<svg width="24" height="24" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><rect x="2" y="2" width="6" height="6" rx="1.6"/><rect x="9" y="2" width="6" height="6" rx="1.6" opacity=".4"/><rect x="16" y="2" width="6" height="6" rx="1.6"/><rect x="2" y="9" width="6" height="6" rx="1.6" opacity=".4"/><rect x="9" y="9" width="6" height="6" rx="1.6"/><rect x="16" y="9" width="6" height="6" rx="1.6" opacity=".4"/><rect x="2" y="16" width="6" height="6" rx="1.6"/><rect x="9" y="16" width="6" height="6" rx="1.6" opacity=".4"/><rect x="16" y="16" width="6" height="6" rx="1.6"/></svg>'''
def url(p):return './' if p['slug']=='index' else p['slug']+'.html'
search=[]
for i,p in enumerate(pages):
    nav=''
    for group in dict.fromkeys(v['group'] for v in pages):
        nav+=f'<div class="nav-group"><p class="nav-label">{group}</p><ul>'
        for v in pages:
            if v['group']==group:
                current=' aria-current="page"' if v==p else ''
                title='Обзор документации' if v['slug']=='index' else v['title']
                nav+=f'<li><a href="{url(v)}"{current}>{title}</a></li>'
        nav+='</ul></div>'
    headings=re.findall(r'<h2 id="([^"]+)">(.*?)</h2>',p['body'])
    toc='<nav class="toc" aria-label="На этой странице"><p class="nav-label">На этой странице</p><ul>'+''.join(f'<li><a href="#{key}">{title}</a></li>' for key,title in headings)+'</ul></nav>'
    pager='<nav class="pager" aria-label="Следующая и предыдущая статья">'
    for label,v in [('Назад',pages[i-1] if i else None),('Далее',pages[i+1] if i+1<len(pages) else None)]:
        pager+=f'<a href="{url(v)}"><span>{label}</span><strong>{v["title"]} {"→" if label=="Далее" else ""}</strong></a>' if v else '<span></span>'
    pager+='</nav>'
    doc=f'''<!doctype html>
<html lang="ru"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>{p['title']} — документация Colloq</title><meta name="description" content="{html.escape(p['desc'],quote=True)}"><meta name="theme-color" content="#0f2d69"><meta name="color-scheme" content="light"><link rel="canonical" href="https://colloq.ru/docs/{'' if p['slug']=='index' else p['slug']+'.html'}"><meta property="og:type" content="article"><meta property="og:locale" content="ru_RU"><meta property="og:title" content="{p['title']} — Colloq"><meta property="og:description" content="{html.escape(p['desc'],quote=True)}"><link rel="icon" href="favicon.svg"><link rel="preload" href="/fonts/hse-sans-400.woff2" as="font" type="font/woff2" crossorigin><link rel="stylesheet" href="docs.css"><script src="docs.js" defer></script></head>
<body class="{'overview' if p['slug']=='index' else 'guide'}"><a class="skip-link" href="#main">К содержанию</a><header class="header"><a class="wordmark" href="/" aria-label="Colloq — главная">{logo}<span>COLLOQ</span></a><a class="header-section" href="./">Документация</a><div class="header-actions"><button class="search-trigger" type="button" hidden><span aria-hidden="true">⌕</span> Поиск <kbd>⌘ K / Ctrl K</kbd></button><a class="back-site" href="/">О проекте <span aria-hidden="true">↗</span></a></div></header>
<div class="layout"><aside class="sidebar"><details class="mobile-nav" open><summary>Разделы документации <span aria-hidden="true">⌄</span></summary><nav aria-label="Разделы документации">{nav}</nav></details><div class="sidebar-foot"><span class="status-dot" aria-hidden="true"></span> На вашем сервере<br><span>Документация текущей реализации</span></div></aside>
<main id="main" tabindex="-1"><div class="article-top"><span class="eyebrow">{p['group']}</span><span class="doc-kind">COLLOQ / GUIDE</span></div><article><h1>{p['title']}</h1><p class="lead">{p['desc']}</p><div class="article-body">{p['body']}</div></article>{pager}<footer class="article-footer"><a href="./">Colloq · Документация</a><span>Учиться вместе. На своём сервере.</span></footer></main>{toc}</div>
<dialog id="search-dialog" aria-labelledby="search-title"><div class="search-heading"><h2 id="search-title">Поиск по документации</h2><button class="close-search" type="button" aria-label="Закрыть поиск">×</button></div><label class="sr-only" for="search-input">Запрос для поиска</label><input id="search-input" type="search" placeholder="Например, резервная копия или Консилиум" autocomplete="off" spellcheck="false" aria-describedby="search-status"><p id="search-status" role="status">Начните вводить название или вопрос.</p><div id="search-results"></div><div class="search-foot"><span>Поиск работает в вашем браузере</span><span><kbd>Esc</kbd> закрыть</span></div></dialog></body></html>'''
    (out/(p['slug']+'.html')).write_text(doc)
    chunks=re.split(r'<h2 id="([^"]+)">(.*?)</h2>',p['body'])
    plain=lambda x:html.unescape(re.sub('<[^>]+>',' ',x)).strip()
    search.append(dict(title=p['title'],section=p['group'],url=url(p),text=p['desc']))
    for n in range(1,len(chunks),3):
        search.append(dict(title=plain(chunks[n+1]),section=p['title'],url=url(p)+'#'+chunks[n],text=re.sub(r'\s+',' ',plain(chunks[n+2]))))
(out/'favicon.svg').write_text(logo.replace('<svg width="24"', '<svg xmlns="http://www.w3.org/2000/svg" width="24"').replace('fill="currentColor"', 'fill="#0f2d69"'))
(out/'search-index.json').write_text(json.dumps(search,ensure_ascii=False,separators=(',',':')))
print(f'Wrote {len(pages)} HTML pages and {len(search)} searchable entries.')
