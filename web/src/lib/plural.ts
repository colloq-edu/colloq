/**
 * Русское числительное — одной строкой из общей земли.
 *
 * Само правило живёт в `shared/plural.ts`: подпись с числом рисует не только
 * комната, но и сервер, собирающий статические страницы для Pages, и своя
 * копия у него уже разъехалась — «5 шага», «21 шагов» в шапке каждой
 * многошаговой страницы на colloq.ru. Файл остаётся ради коротких путей
 * `./plural` у десятка экранов и тестов; ничего своего в нём нет и быть не
 * должно.
 */
export { plural } from '@shared/plural'
