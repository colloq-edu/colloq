# Нейросети на GPU. Образец окружения со срезом видеокарты — можно удалить.
#
# colloq: from base-gpu
#
# Строка выше — не комментарий, а директива: этот слой строится поверх образа
# base-gpu, где torch с CUDA уже стоит. Поэтому здесь только лёгкое сверху, и
# правка этого списка — секунды, а не девять минут на три гигабайта колёс.
#
# Срез видеокарты приходит оттуда же: base-gpu объявляет `# colloq: gpu`, и
# признак наследуется — комната на этом окружении получает срез из KERNEL_GPUS
# на всё время жизни своего контейнера, а если свободных нет, честно не
# открывается. Отката на процессор здесь нет: колёса собраны под CUDA.
#
# `make env-build NAME=gpu` первым делом соберёт base-gpu, если его образа ещё
# нет: это те самые девять минут — но один раз, а не на каждую правку.
transformers>=4.44
accelerate>=0.33
