# Нейросети на GPU. Образец окружения со срезом видеокарты — можно удалить.
#
# colloq: gpu
#
# Строка выше — не комментарий, а директива: комната на этом окружении получает
# срез из KERNEL_GPUS на всё время жизни своего контейнера, а если свободных
# срезов нет — честно не открывается. Отката на процессор здесь нет и быть не
# может: колёса ниже собраны под CUDA.
#
# Ставится поверх базы, поэтому numpy/pandas/matplotlib перечислять не нужно.
# Сборка тянет около семи гигабайт — CUDA едет внутри колёс torch, драйвер
# приходит с хоста через nvidia-container-toolkit, ставить его в образ не надо.
# Первый `make env-build NAME=gpu` идёт долго, дальше образ лежит в кеше docker.

# Обычный индекс pypi, а не .../whl/cpu, как в cv: именно там колёса с CUDA.
torch>=2.4
torchvision>=0.19
transformers>=4.44
accelerate>=0.33
