# Базовое окружение — то, что стоит в каждом семинаре и так.
#
# Здесь намеренно пусто. Ядро всегда ставит kernel/requirements.txt (jupyter,
# numpy, pandas, matplotlib, scikit-learn), а этот файл добавляется сверху.
# Пустой файл значит «ничего сверх базы» — и образ собирается за секунды.
#
# Чтобы завести своё окружение, не редактируйте этот файл:
#
#     make env-new NAME=cv     # создаст kernel/environments/cv.txt
#     make env-use NAME=cv     # соберёт образ и переключит на него ядро
