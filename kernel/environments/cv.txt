# Компьютерное зрение. Пример настоящего окружения — можно удалить.
#
# Ставится поверх базы, поэтому numpy/pandas/matplotlib перечислять не нужно.
# Сборка тянет около двух гигабайт: первый `make env-use NAME=cv` идёт долго,
# дальше образ лежит в кеше docker и переключение мгновенное.

# CPU-сборка: колёса для GPU весят втрое больше и на ноутбуке бесполезны.
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.4
torchvision>=0.19
timm>=1.0
pillow>=10.3
opencv-python-headless>=4.10
