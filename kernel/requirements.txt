jupyter-server>=2.14,<3
ipykernel>=6.29
numpy>=1.26
pandas>=2.2
matplotlib>=3.8
# Интерактивные графики. В базе, а не в окружении, по той же причине, что и
# matplotlib: `px.histogram(df, x=...)` пишут на первом же семинаре по данным, и
# «поставьте себе plotly» — не тот ответ, который даёт преподаватель классу
# посреди пары. Тетрадь рисует фигуру сама (shared/plotly.ts), и ядро для этого
# должно уметь её отдать.
#
# `narwhals` и `packaging` приезжают зависимостями plotly; pandas уже выше.
plotly>=6.0
scikit-learn>=1.4
requests>=2.32
# Форматирование ноутбука кнопкой FORMAT. Живёт в базе, а не в окружении:
# кнопка есть в каждой комнате, значит и инструмент должен быть везде.
black>=24.4
