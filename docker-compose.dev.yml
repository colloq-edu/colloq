# Optional image-build override. Room kernels are always separate containers.
# Host development: make dev; NODE_ENV=development KERNEL_BACKEND=docker npm run dev
services:
  kernel:
    profiles: [image-build]
